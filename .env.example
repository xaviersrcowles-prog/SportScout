# Backend
AI_API_KEY=
AI_PROVIDER=openai
AI_MODEL=gpt-4o-mini
FRONTEND_ORIGIN=*
DATA_FILE=./data/facilities.json
REPORTS_FILE=./data/reports.json

# Frontend (Vite — must be prefixed VITE_ to be exposed to the browser)
VITE_API_BASE_URL=
VITE_MAP_FRAME_URL=/map-frame/index.html
